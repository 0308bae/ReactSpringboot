server {
	listen 80;
	listen [::]:80;

	proxy_set_header host localhost;
	server_name localhost;
	location /hello {
		proxy_pass http://${BACKEND_NAME}:${BACKEND_PORT}/hello;
	}
 	location / {
                proxy_pass http://${FRONTEND_NAME}:${FRONTEND_PORT}/;
        }
}
